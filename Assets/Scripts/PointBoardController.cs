using UnityEngine;
using System.Collections;

public class PointBoardController : MonoBehaviour
{
	PointController controller;
	// Use this for initialization
	void Start ()
	{
		controller = PointController.GetInstance ();
	}
	
	// Update is called once per frame
	void Update ()
	{
	
	}
}
